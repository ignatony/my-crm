spring:
  application:
    name: user_profile
    
    

  datasource:
    url: jdbc:mysql://localhost:3306/my_crm
    username: root
    password: mydatabase

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
